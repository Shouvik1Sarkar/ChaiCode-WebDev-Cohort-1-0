// 1. Create a function that adds two numbers and returns that

function totalCupCakes(tray1, tray2) {
  return tray1 + tray2;
}

console.log(totalCupCakes(12, 20));

// 1. Create a function that multiplies two numbers and returns that

function totalChocolates(chocolates, boxes) {
  return chocolates * boxes;
}

console.log(totalChocolates(12, 20));
